language: shell

stages:
  - build
  - deploy

jobs:
  include:
    - stage: build
      os: linux
      script: ./build.sh
    - stage: build
      os: windows
      script: bash build.sh
    - stage: deploy
      os: linux
      script: ./manifest.sh
